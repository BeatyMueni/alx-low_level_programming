#include "main.h"
#include <stdio.h>

/**
 * main - chek the code
 *
 * Return: Always 0.
 */
int main(void)
{
int alpha;
for (alpha = a; alpha <= z; alpha++)
{
_putchar ("alpha");
_putchar ("\n");
}
return (0);
}
